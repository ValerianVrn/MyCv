@inject IStringLocalizer<Resource> Localizer

<MudDrawer Fixed="true" ClipMode="DrawerClipMode.Always" Anchor="Anchor.End" Open="true" Variant="DrawerVariant.Responsive" Elevation="0" Class="my-6">
    <MudNavMenu Bordered="true">
        <MudText Typo="Typo.h6" Class="px-4">@Localizer["Content"]</MudText>
        <MudDivider Class="my-2" />
        @foreach (var sectionLink in SectionLinks)
        {
            <MudNavLink Href="@sectionLink.Key" Match="NavLinkMatch.All">@sectionLink.Value</MudNavLink>
        }
    </MudNavMenu>
</MudDrawer>

@code {
    [Parameter]
    public required Dictionary<string, string> SectionLinks { get; set; }
}
